---

---

<section class="destaque">
  <div class="container">
    <p class="destaque__frase">
      Quando cuidar de quem você ama exige mais do que amor, a Cuida entende.
    </p>
  </div>
</section>

<style>
  /* Trecho de maior carga emocional da página: respiro generoso é parte do efeito
     (docs/front-end-spec-mobile.md §4 bloco H) */
  .destaque {
    background: var(--bg-destaque);
    color: var(--texto-sobre-lilas);
    padding-block: var(--esp-secao);
  }

  .destaque__frase {
    font-family: var(--font-display);
    font-size: var(--fs-display-lg);
    line-height: var(--lh-display);
    text-align: center;
    max-width: 28ch;
    margin-inline: auto;
  }
</style>
